/*
import {
  socket as socketStore,
  client as clientStore,
  role as roleStore,
} from "./stores.js";
// import { get } from 'svelte/store';
// const value = get(store);

var socket;
socketStore.subscribe((value) => {
  socket = value;
});

var client;
clientStore.subscribe((value) => {
  client = value;
});

var role;
roleStore.subscribe((value) => {
  role = value;
});

export function updateClient() {
  console.debug(`${role} >>> registerUpdate`);
  socket.emit("registerUpdate", client);
}
*/
